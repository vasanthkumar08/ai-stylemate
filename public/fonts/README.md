Place licensed font files here to activate the production typography system:

- `PeaceSans.woff2` for hero, brand, and main dashboard headings.
- `Avalance.woff2` for section, pricing, feature, and admin headings.

The app uses SSR-safe fallbacks until these files are present.
